packages:
  - apps/*
  - packages/*
allowBuilds:
  # core-js only prints a funding banner from its postinstall script; its
  # runtime modules are already shipped in the package, so no build is needed.
  core-js: false
  # These pinned tools install their platform binaries for self-host builds.
  esbuild: true
  workerd: true
minimumReleaseAgeExclude:
  - vite@8.2.1
  - svg2pdf.js@2.8.0
